pipeline {
    agent any

    stages {

        stage('Checkout Code') {
            steps {
                git branch: 'bug-002',
                    url: 'https://github.com/veerendrabijib/practice-session1.git'
            }
        }

    }

    post {
        always {
            publishHTML([
                allowMissing: false,
                alwaysLinkToLastBuild: true,
                keepAll: true,
                reportDir: 'bijib',
                reportFiles: 'demo.html,style.css,script.js',
                reportName: 'Demo HTML Page',
                useWrapperFileDirectly: true
                 ])

        }
        success {
            echo 'Build Successful'
        }
        failure {
            echo 'Build Failed'
        }
    }
}
